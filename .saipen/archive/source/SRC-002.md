PROBLIP ANDROID — RECOVER AFTER COMPACTION AND COMPLETE THE CURRENT FEATURE WAVE

ROLE

Resume from the ACTUAL repository state.

Do not rely on compacted conversation memory.

The current repository snapshot has been inspected externally.

==================================================
RECOVERY FACTS
==============

Current main ref:

6c123e8a2ce551817efc7962f60af56af2189c1c

The repository is still at the completed localization/help/EXIT wave.

The requested statistics/reward/glow wave has NOT been implemented at all.

Verified current state:

* no statistics package
* no BlipStats model/repository
* no earnedPremium
* no feature_blip_glow
* no show-blip-counter preference
* no glow-enabled preference
* no Today / Week / Month / Total UI
* PremiumAccess currently contains only:

  * activeTrials
  * developerAccess
* TrialAccess feature ids currently contain only:

  * feature_manual_interval
  * feature_pulse_interval
* SettingsRepository contains no counter/glow settings
* README still says:
  "No counters, cooldowns or accounts."
* .saipen/STATE.md is stale at the previous DONE state from September 5

Therefore:

DO NOT search for a half-finished stats implementation.

There is none.

Start this wave cleanly from the current accepted 6c123e8 product state.

==================================================
SAIPEN RECOVERY FIRST
=====================

Before implementation, register a NEW work item.

Suggested:

T-027 [P1] Blip statistics + 100K earned Premium + Blip Glow

Update STATE / BOARD / LOG honestly.

Do not pretend the old DONE state describes the new user request.

Also ingest the new user request as a newer source requirement / append.

IMPORTANT SOURCE-CONTRACT SUPERSESSION:

An old trial requirement says:

"No cooldowns, counters, accounts, device IDs, server validation or anti-abuse logic."

The NEW user instruction explicitly requires a product blip counter/statistics.

Do not treat the historical word "counters" as blocking this wave.

Supersede ONLY that part.

The intended remaining old rule is still:

* no trial-use cooldown
* no trial abuse counter
* no account
* no device ID
* no server validation
* no anti-abuse system

The new Blip Statistics counter is PRODUCT STATISTICS, not trial anti-abuse logic.

Record that supersession explicitly instead of silently rewriting historical evidence.

==================================================
CURRENT IMPLEMENTATION TARGET
=============================

Implement exactly:

1. successful-blip statistics
2. TODAY
3. THIS WEEK
4. THIS MONTH
5. TOTAL
6. permanent local Premium at 100,000 successful blips
7. Main counter visibility toggle
8. premium BLIP GLOW
9. reusable 5-minute BLIP GLOW trial

No additional product feature in this wave.

100,000 means:

100_000 successful blips

NOT 100,000,000.

Constant:

PREMIUM_REWARD_BLIPS = 100_000L

==================================================
P0 — AUTHORITATIVE SUCCESS BOUNDARY
===================================

Count only REAL successful playback.

Current production path is approximately:

BlipPlayer {
audio.play().also(ProblipSession::reportPlayback)
}

Replace/refine this in ONE place so successful playback performs:

1. increment local statistics
2. report the existing ProblipSession playback signal

Conceptually:

val succeeded = audio.play()

if (succeeded) {
stats.recordSuccessfulBlip()
ProblipSession.reportPlayback(true)
}

succeeded

Do NOT increment on:

* scheduled attempt
* failed audio.play()
* START
* glow
* playback lamp
* widget
* notification

One real successful scheduler playback = one blip.

The ~500 ms initial successful blip counts.

Do not create a second playback callback/event system.

==================================================
P0 — STATS ARCHITECTURE
=======================

Create one small dedicated package, for example:

stats/

Suggested responsibility split:

BlipStats.kt

* pure snapshot/model
* calendar keys
* rollover
* reward threshold
* sanitization

BlipStatsRepository.kt

* Preferences DataStore
* in-memory StateFlow
* persistence

Do NOT use:

Room
SQLite wrapper
Proto DataStore
WorkManager
AlarmManager
backend
analytics SDK

Use a separate Preferences DataStore:

problip_stats

Do NOT add fast-changing counts to SettingsRepository.Settings.

SettingsRepository is already part of active service configuration/access flows and
must not emit every time a blip happens.

==================================================
STATS SNAPSHOT
==============

Track only current aggregate buckets.

Conceptually:

BlipStats(
todayCount: Long,
weekCount: Long,
monthCount: Long,
totalCount: Long,
earnedPremium: Boolean,
)

Persist the period identity required to know whether each bucket is current:

day key
ISO week key
month key

Do NOT store every blip timestamp.

Use Long.

Sanitize negative/corrupt values.

==================================================
PERIOD SEMANTICS
================

TODAY:
current local calendar day

THIS WEEK:
Monday through Sunday

THIS MONTH:
current local calendar month

TOTAL:
lifetime local successful blips

Use:

Clock
ZoneId
java.time

Make Clock injectable for tests.

Do not make tests depend on the machine's current date.

At midnight/day/week/month rollover:

display stale previous-period aggregate as 0 immediately,
even before the first new blip.

On first successful blip in the new period:

new current bucket becomes 1.

TOTAL never rolls over.

==================================================
PERSISTENCE STRATEGY
====================

Playback must not wait on DataStore disk I/O.

recordSuccessfulBlip() must update in-memory state immediately and cheaply.

Persist asynchronously.

Use a bounded/simple batching policy.

Recommended:

* immediate StateFlow increment
* persist after a small bounded batch and/or short bounded interval
* flush during normal STOP / EXIT / service teardown where practical
* IMMEDIATELY persist when crossing 100,000

Do not create a generic journaling framework.

A tiny loss window for ordinary display statistics after abrupt process kill is
acceptable.

Loss of the already-crossed 100K entitlement is NOT acceptable once persisted.

Stats persistence failure must never:

* produce session ERROR
* stop scheduler
* stop audio
* release WakeLock
* stop foreground service

Audio is primary.
Stats are secondary.

==================================================
P0 — 100K EARNED PREMIUM
========================

At:

total >= 100_000

set:

earnedPremium = true

This flag is irreversible.

Once true, it remains true across:

* day/week/month rollover
* process restart
* reboot
* app update
* Developer Access expiry
* trial expiry
* Billing refresh
* purchase refund
* Reset Temporary Access

Do not make this a fake Google Play purchase.

NEVER:

* add fake product ids to ownedProducts
* mutate BillingRepository ownership
* call purchase APIs
* display OWNED solely because of 100K

Use a distinct access source:

earnedPremium

==================================================
PREMIUM ACCESS INTEGRATION
==========================

Extend PremiumAccess:

PremiumAccess(
activeTrials = ...,
developerAccess = ...,
earnedPremium = ...,
)

Conceptual access:

free ||
owned ||
activeTrial ||
developerAccess ||
earnedPremium

earnedPremium is a GLOBAL Premium override.

It grants:

* premium sounds
* premium themes
* MANUAL
* PULSE
* BLIP GLOW

Do not scatter:

total >= 100_000

through UI/catalog/service code.

Only the statistics/reward authority decides earnedPremium.

==================================================
COLD START REQUIREMENT
======================

This is important.

Current service already waits for initialized Billing and temporary access before
the FIRST effective session.

100K earned Premium must participate in that same correctness guarantee.

If earnedPremium was already persisted before process death:

a cold START, including widget START, must NOT briefly:

* fall back to Original premium-inaccessible sound
* fall back from MANUAL
* fall back from PULSE

while the stats DataStore is still loading.

Introduce a small readiness signal for stats/earned entitlement.

Integrate it with existing cold-start/access initialization.

Do not regress the existing ColdStart fix.

==================================================
TRIAL COORDINATOR INTEGRATION
=============================

Keep one effective PremiumAccess state.

Preferred approach:

feed the initialized earnedPremium state into the current access coordinator or
equivalent single authority.

Do not make every caller manually combine:

TrialCoordinator.access + stats.earnedPremium

in unrelated code.

Preserve the current access architecture.

==================================================
ACCESS LABELS
=============

Add semantic:

EARNED

to the existing AccessLabel model.

Priority for premium content:

FREE -> no premium label / INCLUDED where appropriate

real Play ownership -> OWNED

earned 100K Premium -> EARNED

Developer Access -> DEV

running trial -> TRIAL mm:ss

otherwise -> TRY 5 MIN / BUY

Do not label EARNED as OWNED.

If the user has both Play ownership and earnedPremium:

OWNED may remain higher priority for genuinely owned content.

For other premium content unlocked globally by 100K:

EARNED.

==================================================
P0 — BLIP GLOW
==============

Stable feature id:

feature_blip_glow

Add it to TrialAccess.FEATURE_IDS.

It rides the existing:

theme_pack / Customization Pack

No new Play product.

Access is granted by:

* theme_pack ownership
* active 5-minute feature_blip_glow trial
* Developer Access
* earnedPremium

Add:

PremiumAccess.grantsBlipGlow(ownsCustomizationPack)

or equivalent.

==================================================
GLOW PREFERENCE
===============

This is a slow-changing user preference, so it MAY live in SettingsRepository.

Add:

blipGlowEnabled: Boolean = true

Default:

ON

ON means:

effect is enabled when access permits.

It does NOT grant access.

Persist user OFF choice.

==================================================
GLOW ANIMATION
==============

Reuse the existing:

ProblipSession.blips

signal that already drives the successful-playback lamp.

Do NOT create another event bus.

When:

* Main is visible/resumed
* a real blip succeeds
* preference is ON
* effective access grants Glow

animate the whole Main background softly.

Maximum accent overlay alpha:

0.25f

Suggested timing:

quick rise
smooth decay
~150–350 ms total

Use the active Wintage accent/highlight token.

Do NOT:

* flash white
* modify palette constants
* wake the screen
* flash widget
* update notification
* run animation while Main is absent
* replay old event when returning to Main

Trial expiry simply prevents future glow events.

Do not restart/stop the scheduler.

==================================================
GLOW TRIAL
==========

Reuse existing trial machinery.

Duration:

300_000 ms

Reusable.
No cooldown.
Re-tap while active does not extend.
Persisted expiry.
No scheduler restart on expiry.

Settings presentation should stay compact.

Example:

BLIP GLOW             ON
PREMIUM        TRY 5 MIN

During trial:

BLIP GLOW             ON
TRIAL 04:21

Other labels:

OWNED
EARNED
DEV

No dedicated Glow screen.

==================================================
P0 — COUNTER VISIBILITY
=======================

Add slow preference to SettingsRepository:

showBlipCounter: Boolean = true

Default ON.

This affects ONLY Main visibility.

It must NEVER:

* disable recording
* pause statistics
* prevent 100K reward

Main displays only:

BLIPS 73,412

or localized equivalent.

Today/week/month belong in Settings.

Main no-scroll contract remains absolute.

==================================================
SETTINGS STATISTICS
===================

Add one compact section to the existing Settings screen:

STATISTICS

TODAY
THIS WEEK
THIS MONTH
TOTAL

PREMIUM REWARD
73,412 / 100,000

or after unlock:

PREMIUM REWARD
EARNED

SHOW BLIP COUNTER [ON/OFF]

VISUAL

BLIP GLOW [ON/OFF]
access/trial label

Do not add another screen.

Do not build charts.

No streaks.

No graphs.

No reset stats button.

==================================================
NO RESET STATISTICS
===================

Do not expose a production reset.

Lifetime total now controls permanent Premium access.

Reset Temporary Access continues to clear ONLY:

* five-minute trials
* Developer Access

It must NOT clear:

* stats
* earnedPremium
* Play purchases

==================================================
LOCALIZATION
============

Add EN / RU / ET / JA resources for:

STATISTICS
TODAY
THIS WEEK
THIS MONTH
TOTAL
BLIPS
SHOW BLIP COUNTER
PREMIUM REWARD
EARNED
BLIP GLOW
Glow trial/access copy
Help statistics note

Use locale-aware numeric formatting.

Do not hardcode UI English.

==================================================
HELP
====

Add a very short statistics note:

* successful blips are counted
* Today / Week / Month / Total
* 100,000 total successful blips unlock Premium locally forever
* progress is local app data because Problip has no account/backend

Do not document the hidden Developer Access chord.

==================================================
MAIN NO-SCROLL
==============

Do not regress the accepted Main layout.

Test counter visible with:

EN
RU
ET
JA

1.0 font scale
~1.3 font scale

STOPPED
STARTING
RUNNING
ERROR
MANUAL
PULSE

MINIMIZE
EXIT
START/STOP

must remain immediately visible.

Never add verticalScroll/LazyColumn to Main.

==================================================
REQUIRED TESTS
==============

Add deterministic coverage at minimum for:

STATISTICS

1. successful blip increments today/week/month/total
2. failed blip increments none
3. day rollover
4. Monday week rollover
5. month rollover
6. year rollover
7. stale period displays zero before next blip
8. negative/corrupt persisted values sanitize
9. Long counts do not become negative

100K

10. 99_998 -> 99_999 not earned
11. 99_999 -> 100_000 becomes earned
12. 100_000 -> 100_001 remains earned
13. earned survives repository reload
14. temporary reset does not revoke earned
15. Billing ownership loss does not revoke earned
16. earned grants premium sound/theme/MANUAL/PULSE/GLOW
17. earned does not mutate Play ownership
18. cold process start respects already-earned Premium

COUNTER

19. hidden counter still records
20. hidden counter can still trigger reward

GLOW

21. default preference ON
22. no access -> no glow eligibility
23. theme_pack -> glow
24. trial -> glow
25. Developer Access -> glow
26. earned -> glow
27. preference OFF -> no glow
28. expired trial -> no new glow, session unchanged
29. failed playback -> no stats increment / no successful-blip UI signal

ACCESS LABEL

30. OWNED outranks global earned on actually owned item
31. EARNED outranks DEV/TRIAL for non-owned premium content
32. DEV outranks trial where not earned

COLD START

33. earned premium premium-sound selection works on first plan
34. earned premium MANUAL works on first plan
35. earned premium PULSE works on first plan

==================================================
NEGATIVE CONTROL
================

Prove important tests bite.

At least temporarily break and restore:

* reward threshold
* successful-only stats hook
* earnedPremium access gate

Show expected test failures, then restore green state.

Do not leave mutation code.

==================================================
PERFORMANCE / POWER CONTRACT
============================

Do not add:

* permission
* WakeLock
* AlarmManager
* WorkManager
* periodic OS worker
* per-second persistence loop
* widget update every blip
* notification update every blip
* network

Do not modify:

BlipScheduler timing
DelayBoundary
WakeLock policy
PULSE phase
Manual resolution
SoundPool preparation
FGS lifecycle

These paths already passed physical testing.

==================================================
SAIPEN CLOSEOUT
===============

Update:

README
.saipen/STATE.md
.saipen/BOARD.md
.saipen/LOG.md

The new wave must no longer leave STATE at the old September 5 DONE snapshot.

Close T-027 only after full verification.

Do not close unrelated Play/hosting/device release gates.

==================================================
BUILD GATE
==========

Run:

gradlew test assembleDebug assembleRelease lint lintVitalRelease --no-daemon

Report:

* unique JVM tests
* suites
* failures/errors
* lint result
* debug APK size/path
* release APK size/path

Export fresh root APKs from this current build.

==================================================
COMMIT
======

Create ONE focused commit:

feat: add blip statistics rewards and premium glow

No squash of accepted commits.
No push.

Final git status should be clean for product paths.

==================================================
FINAL REPORT
============

Return:

1. exact recovery point
2. files added/changed
3. successful-playback counting boundary
4. Today/Week/Month/Total semantics
5. persistence/batching policy
6. 100K earned entitlement implementation
7. proof Billing ownership remains separate
8. cold-start earned-Premium handling
9. counter visibility behaviour
10. Glow + 5-minute trial behaviour
11. localization changes
12. unique test count
13. negative-control evidence
14. build/lint evidence
15. fresh APK sizes
16. commit hash
17. git status
18. remaining physical checks

STOP.

Do not start the older sounds/logo/widget-branding backlog in this wave.
That remains a separate follow-up.
