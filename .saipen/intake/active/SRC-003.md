PROBLIP ANDROID — T-28 RELEASE CONTENT & IDENTITY

ROLE

Continue from the CURRENT accepted repository state.

Current accepted commits include:

4e7dcf4
afd9ed1

T-27 / T-27.1 are COMPLETE.

Do NOT reopen statistics, earned Premium, Glow, scheduler, WakeLock, Billing,
trials or cold-start unless a regression test from THIS wave proves an actual bug.

This wave prepares the actual product identity and content surface for RC1.

Scope:

1. final sound-content readiness
2. remove the duplicate Custom theme
3. make widget 1x1-first and sensibly resizable
4. create final Problip visual identity assets
5. launcher/adaptive/monochrome icons
6. in-app brand mark
7. correct notification small icon
8. release-content cleanup
9. fresh RC debug artifact

Do NOT start signing / Play publishing in this wave.

==================================================
CURRENT VERIFIED BASELINE
=========================

Current HEAD:

afd9ed128b419eb7b188ac8b1afac535e6834b33

T-27.1 evidence:

251 unique JVM tests
0 failures
0 errors
30 suites
lint 0 errors

BlipStatsRepository now correctly guarantees:

record
-> earnedPremium
-> ready

Do not touch this architecture.

README still says 248 tests.

Correct that stale number during this wave.

==================================================

1. FINAL SOUND CONTENT
   ==================================================

Current catalog is still placeholder content:

sound_original -> Original Blip
sound_glass -> Glass
sound_wood -> Wood
sound_soft_bell -> Soft Bell
sound_bonk -> Bonk
sound_space -> Space

Current premium WAVs were generated by:

scripts/make_placeholder_sounds.py

They are NOT release assets.

==================================================
DO NOT INVENT THE USER'S FINAL SOUNDS
=====================================

The currently supplied repository does NOT contain the user's final curated sound
set.

Do not synthesize replacements.

Do not choose random sounds.

Do not download stock audio.

Do not rename placeholder sounds and pretend they became final.

Search the CURRENT repository/reference/source intake for explicitly supplied
final user-selected WAV assets.

If those assets are not present:

mark ONLY the audio-replacement subtask:

HUMAN_ASSET_BLOCKED

and continue every other T-28 item.

Do not block branding/widget/theme work because the external WAV files are absent.

==================================================
FINAL SOUND IMPORT CONTRACT
===========================

When final WAV assets are supplied:

preserve existing stable product IDs where practical:

sound_glass
sound_wood
sound_soft_bell
sound_bonk
sound_space

The display name and underlying WAV may change.

Stable IDs matter because they already participate in:

* settings persistence
* trials
* Billing products
* selection pools

Do NOT casually change Play product IDs just because the sound name changes.

If the user's final set has a different number of premium sounds:

STOP and explicitly map:

old stable id
-> final sound
-> display name
-> asset filename

before changing ProductCatalog.

Do not silently add/remove Billing products.

==================================================
AUDIO QA
========

For every supplied final WAV verify:

* valid WAV
* decodes through SoundPool
* short enough for this product
* no clipping
* no accidental silence
* reasonable perceived loudness compared with Original
* no absurd sample rate/channel encoding
* no giant file size
* no leading/trailing dead air that changes perceived timing

Do not normalize destructively without recording what was changed.

Prefer source assets already prepared by the user.

==================================================
AUDIO SOURCE LEDGER
===================

Add:

reference/audio/SOURCES.md

For each final sound:

* stable sound id
* user-facing name
* source filename
* origin / author
* license or "original/user supplied"
* transformation performed, if any

No legal archaeology six months after release.

If final assets are still missing, create the ledger structure and mark entries
PENDING USER ASSET.

Keep README's placeholder warning until real final WAVs are actually present.

Remove:

scripts/make_placeholder_sounds.py

ONLY after all placeholder premium WAVs have genuinely been replaced.

==================================================
2. REMOVE DUPLICATE CUSTOM THEME
================================

Current:

ThemeCatalog.CUSTOM
id = theme_wintage_custom
display name = Custom

PALETTE_CUSTOM is currently identical to PALETTE_CLASSIC / Golden Default.

Therefore renaming it would create two differently-named identical themes.

Remove `Custom` from the user-visible theme catalog.

Preferred release result:

15 visible Wintage themes total:

1 free Golden Default
14 premium non-duplicate palettes

Do not modify the remaining authoritative palette colors.

==================================================
CUSTOM MIGRATION
================

The app has not publicly shipped yet, but local testers may already have:

theme_wintage_custom

persisted.

Handle that stored ID safely.

Preferred:

theme_wintage_custom
-> effective Golden Default / theme_classic

Do not crash.
Do not leave an invisible selected theme.
Do not add a migration subsystem.

A tiny legacy-id normalization is enough.

Remove:

ThemeCatalog.CUSTOM
PALETTE_CUSTOM
related duplicate-only tests

Update tests to pin:

* visible catalog count
* no duplicate Custom entry
* legacy theme_wintage_custom resolves safely to Golden Default

Update README:

16 themes
-> 15 themes

if Custom is removed as directed.

==================================================
3. WIDGET — 1x1 FIRST
=====================

Current widget:

targetCellWidth = 2
targetCellHeight = 1

minWidth = 180dp
minHeight = 70dp

resizeMode = horizontal

Current layout always assumes:

title + status
then a separate START/STOP button.

Change the widget so its primary supported size is:

1x1

The smallest form must remain useful.

==================================================
1x1 DESIGN
==========

At true compact size prioritize:

PROBLIP mark / identity
session state
one large START / STOP target

Do not attempt to reproduce the Main screen.

Preferred compact hierarchy:

small brand mark / P
state hint
[ START ]

or an even cleaner single-surface treatment.

The whole useful action region should have a reasonable touch target.

Widget remains:

START / STOP ONLY

Do NOT add:

* EXIT
* MINIMIZE
* interval picker
* sound picker
* stats counter
* Glow
* Billing
* trial controls

==================================================
RESIZABLE WIDGET
================

Allow sensible resizing without fanaticism.

Target support:

1x1
2x1
2x2

and launcher variations close to those bounds.

Use:

resizeMode="horizontal|vertical"

with sensible minResizeWidth / minResizeHeight if needed.

On Android launchers that provide widget size information, adapt the RemoteViews
presentation where reasonably simple.

Do NOT create a mini responsive framework.

Preferred:

one compact layout
plus at most one expanded layout/visibility arrangement.

Examples:

1x1:
brand mark + START/STOP

2x1:
PROBLIP + OFF/RUNNING + larger START/STOP

2x2:
same information with more breathing room

Additional space must NOT invent additional product functionality.

==================================================
WIDGET STATE CONTRACT
=====================

Preserve exactly:

STOPPED -> START
STARTING -> STOP
RUNNING -> STOP
ERROR -> START

Keep:

* real ProblipSession state
* no polling
* no second scheduler
* explicit user START can start FGS
* exceptions remain safely handled
* title/brand may open Main

Add tests for any new pure size/presentation policy.

No Robolectric solely to count pixels.

==================================================
4. CREATE PROBLIP VISUAL IDENTITY
=================================

Create one small coherent Problip mark.

This is a tiny utility, not a venture-capital fintech rebrand.

The identity should fit the existing visual language:

* Golden Default
* Wintage / compact desktop-tool feeling
* simple geometric mark
* recognizable at 16–24 px
* readable in monochrome
* not dependent on tiny lettering

Preferred concept:

a geometric "BLIP" pulse mark built around a compact central square/dot and one or
two surrounding signal/pulse shapes.

It should visually suggest:

sound cue
pulse
momentary blip

without looking like:

Wi-Fi
Bluetooth
Spotify
alarm clock
medical ECG
generic play button

Keep it extremely simple.

==================================================
SINGLE SOURCE SHAPE
===================

Prefer one canonical vector geometry that can be adapted for:

* launcher foreground
* monochrome themed icon
* in-app brand mark
* notification small icon

Do not make four unrelated logos.

Different containers/masks are fine.

==================================================
5. ADAPTIVE LAUNCHER ICON
=========================

Current AndroidManifest has NO explicit:

android:icon

or:

android:roundIcon

Fix this.

Create proper Android launcher assets.

Required:

adaptive icon for API 26+
foreground layer
background layer

Use Golden Default identity colors for the normal launcher icon.

Keep important foreground geometry inside adaptive safe bounds.

Do not put the word PROBLIP inside the launcher icon.

It will become microscopic soup.

==================================================
LEGACY / FALLBACK LAUNCHER
==========================

Provide appropriate fallback mipmap resources for launchers/devices that require
them.

Manifest must reference the proper launcher icon resources.

Do not leave Android's generic fallback application icon.

==================================================
6. MONOCHROME THEMED ICON
=========================

Add Android monochrome icon support for themed icons.

The monochrome layer must be a real single-color silhouette/mask.

No embedded Golden Default colors.

No gradients required.

It must still resemble the Problip mark at launcher scale.

==================================================
7. IN-APP BRAND MARK
====================

Current Main uses the text brand:

PROBLIP

Preserve the word PROBLIP.

Add the small mark as part of the existing header identity.

Do NOT increase header height materially.

Do NOT break the secret Developer Access gesture.

CRITICAL:

The existing hidden chord depends on holding the PROBLIP title for >=20 seconds.

The title/brand interaction target must preserve the EXACT accepted gesture.

Safe options:

mark + PROBLIP inside the same existing gesture hit target

or:

mark decorative, title keeps the current exact gesture target

Do NOT let tapping the new mark accidentally unlock Developer Access.

Do NOT make the header taller enough to reintroduce Main scrolling.

==================================================
8. NOTIFICATION SMALL ICON
==========================

Current:

ic_stat_problip.xml

uses two opaque colored rectangles:

outer gold square
inner dark square

That is incorrect for Android notification-small-icon semantics.

Android uses the icon primarily as an alpha mask and applies system tinting.

A different opaque fill color does NOT create a transparent hole.

Replace it with a proper monochrome alpha silhouette.

Use:

white/opaque geometry
transparent background

If the design needs a central hole, make it genuinely transparent using correct
vector path geometry/fill rules.

No colored notification small icon.

Verify at:

24dp
status-bar scale
light system UI
dark system UI

Keep:

.setSmallIcon(R.drawable.ic_stat_problip)

or rename only if there is a strong reason.

==================================================
9. BRAND CONSISTENCY
====================

The same Problip identity should be recognizable across:

launcher
Main header
widget
notification

Do not force the full launcher container shape into the notification icon.

Use the same underlying mark.

Widget may use the compact mark rather than spelling PROBLIP when 1x1 space is
tight.

==================================================
10. ACCESSIBILITY
=================

Decorative logo:

do not add noisy duplicate screen-reader announcements.

Interactive brand/title:

retain the existing meaningful PROBLIP semantics.

Widget START/STOP text must remain understandable without relying only on color or
the logo.

==================================================
11. RELEASE CONTENT CLEANUP
===========================

Search the project for:

placeholder
temporary
TODO
FIXME
sample
debug
synthesized
stand-in
Custom
16 themes
248 unique

Classify every hit.

Do NOT blindly delete developer comments.

Fix user/release-facing stale material.

At minimum:

README 248
-> 251 or the new final test count after T-28

README theme count
-> 15 if Custom removed

README placeholder audio warning:
keep it if final curated assets are still absent
remove it only when replacement is real

README Next code wave:
update honestly

==================================================
12. DO NOT CHANGE CORE BEHAVIOUR
================================

Do NOT modify unless a regression forces it:

BlipScheduler
DelayBoundary
WakeLock
ProblipService lifecycle
SoundPool async-load correctness
PULSE timing
Manual timing
stats persistence
100K reward
PremiumAccess semantics
trial expiry
Developer Access
BillingRepository
ColdStart
Blip Glow event source

T-28 is CONTENT / IDENTITY / WIDGET PRESENTATION.

Not architecture.

==================================================
13. NO NEW PERMISSIONS / DEPENDENCIES
=====================================

Add:

zero Android permissions

Prefer:

zero new dependencies

VectorDrawable / adaptive icon / RemoteViews are enough.

Do not pull an icon framework into the APK to draw one square and a pulse.

==================================================
14. TESTS
=========

Preserve all current 251 tests.

Add focused tests for:

THEMES

1. visible theme catalog no longer exposes Custom
2. expected final visible theme count
3. legacy theme_wintage_custom safely falls back to Golden Default
4. every visible theme has a palette
5. no remaining visible duplicate of Golden Default caused by Custom

WIDGET

6. state mapping unchanged
7. compact presentation policy remains START/STOP-only
8. resize policy does not invent additional actions if pure sizing logic is
   extracted

SOUNDS

When final assets are actually supplied:

9. every SoundCatalog entry points to an existing raw resource
10. every ProductCatalog premium sound id resolves to exactly one SoundCatalog
    entry
11. no placeholder catalog entry remains unintentionally
12. free Original remains free and playable

BRANDING

Prefer resource/build verification rather than fragile pixel tests.

Verify manifest launcher icon references exist.

==================================================
15. BUILD / RESOURCE GATE
=========================

Run:

gradlew test assembleDebug assembleRelease lint lintVitalRelease --no-daemon

Additionally verify packaged debug APK contains:

* launcher adaptive icon resources
* monochrome icon resource
* Problip brand vector
* corrected notification icon
* widget 1x1 provider metadata
* no `theme_wintage_custom` visible catalog entry

If final WAVs are supplied:

verify their resource names and catalog mappings in the APK.

Report:

* unique JVM tests
* suites
* failures/errors
* lint
* debug APK size
* release APK size

==================================================
16. PHYSICAL CHECKLIST FOR USER
===============================

Do NOT claim these from JVM tests.

User will physically verify:

LAUNCHER

* icon looks correct
* adaptive masking does not crop mark
* themed monochrome icon works

MAIN

* brand mark looks correct
* no new scroll
* secret 20-second chord still behaves exactly as before

NOTIFICATION

* status bar icon is recognizable
* no solid square/blob
* light/dark status bar rendering is acceptable

WIDGET

* can place approximately 1x1
* START/STOP is easy to hit
* resize 2x1 and 2x2 remains clean
* launcher resize does not clip text
* state remains synchronized

THEMES

* Custom is absent
* previously-selected Custom safely becomes Golden Default

SOUNDS

* final sounds subjectively match intended loudness/character
* multi-sound pool behaves normally

==================================================
17. SAIPEN
==========

Create:

T-28 [P1] Release Content & Identity

Do not reopen T-27.

If final user WAV assets are absent:

record the audio portion honestly as:

HUMAN_ASSET_BLOCKED

while completing every independent T-28 subtask.

Do not mark the whole T-28 DONE until the actual release audio assets are present,
unless SAIPEN splits the asset dependency into a separate child gate.

Preferred split if necessary:

T-28A Identity / widget / theme cleanup
T-28B Final curated audio assets [BLOCKED: user assets]

Do not manufacture evidence for T-28B.

==================================================
18. GIT
=======

One focused implementation commit for completed code/assets, suggested:

feat: finalize Problip release identity and widget

If final audio arrives in a later pass, use a separate commit:

feat: replace placeholder sounds with final audio

Do NOT amend/squash:

afd9ed1
4e7dcf4

No push.

==================================================
19. FINAL REPORT
================

Return:

1. T-28 SAIPEN state
2. final visible theme count
3. Custom legacy handling
4. widget supported size behavior
5. launcher icon architecture
6. monochrome icon
7. in-app brand integration
8. notification icon correction
9. final-sound status:
   COMPLETE or HUMAN_ASSET_BLOCKED
10. exact sound ID -> asset/name mapping if complete
11. release-content cleanup performed
12. unique JVM test count
13. lint/build evidence
14. APK sizes
15. commit hash(es)
16. git status
17. exact remaining human/device checks

STOP.

Do not proceed into signing, Play Console or production rollout in this wave.
