*.iml
.gradle/
.idea/
.kotlin/
build/
local.properties
captures/
.externalNativeBuild/
.cxx/
desktop.ini
Thumbs.db
.DS_Store
*.apk
# Unsigned repository-verification bundle (docs/release-checklist.md); never an
# upload artifact - Play gets the signed AAB built at release time.
*.aab

# Release signing material must never enter Git (docs/release-signing.md).
keystore.properties
*.jks
*.keystore

# Agent/tool local runtime state (not project source)
.codebase-memory/
.freebuff/
